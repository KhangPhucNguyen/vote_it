<!DOCTYPE html>
<html>
<head>
  <title>Vote It</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-inverse navbar-static-top">
  <div class="navbar-inner">
    <div class = 'container'>
      <a class="brand" href="/">VOTE IT</a>
      <ul class="nav">
        <%
          unless @menus.nil?
            @menus.each do |menu|
        %>
          <li><a href="<%=menu[:action]%>"><%=menu[:name]%></a></li>
        <%
            end
          end
        %>
        <% if signed_in? %>
          <li><a href="/signout">Signout</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
<div class="row-fluid">
  <% if flash[:message] %>
    <div class="alert alert-info">
      <%= flash[:message] %>
    </div>
  <% end %>
</div>
<div class='break'></div>

<%= yield %>

</body>
</html>
